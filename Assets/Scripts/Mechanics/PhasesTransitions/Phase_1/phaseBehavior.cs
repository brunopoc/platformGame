using UnityEngine;
using System.Collections;

public class phaseBehavior : MonoBehaviour {

    SpriteRenderer renderer1;
    SpriteRenderer renderer2;
    SpriteRenderer renderer3;
    SpriteRenderer renderer4;

    public player_onmap player_onmap;

    void Start (){
	
    }

    void Update (){
	    /*
	    if(player_onmap.phase1_clear == true){
		    renderer1.color = new Color(0f, 0f, 0f, 1f);
		    renderer2.color = new Color(0f, 0f, 0f, 1f);
		    renderer3.color = new Color(0f, 0f, 0f, 1f);
		    renderer4.color = new Color(0f, 0f, 0f, 1f);
	    }
        */
    }
}